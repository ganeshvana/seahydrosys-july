<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_form_inerit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//field[@name='order_line']//tree//field[@name='price_unit']"
                position="before"
            >
                <field name="line_weight" optional="show" />
                <field name="line_volume" optional="show" />
            </xpath>
            <field name="notes" position="after">
              
                <!-- <div
                    name="total_weight"
                    attrs="{'invisible':[('weight_uom_name', '=', '')]}"
                > -->
                 <div class="o_row">
                   <label
                    for="total_weight"
                    attrs="{'invisible':[('weight_uom_name', '=', '')]}"
                />
                    <field name="total_weight" style="width: 70%;" class="oe_inline" />
                    <field name="weight_uom_name" style="width: 30%;" class="oe_inline"/>
                </div>
                   
                <!-- </div> -->
                
                <!-- <div
                    name="total_volume"
                    attrs="{'invisible':[('volume_uom_name', '=', '')]}"
                > -->
                <div class="o_row">
                <label
                    for="total_volume"
                    attrs="{'invisible':[('volume_uom_name', '=', '')]}"
                />
                    <field name="total_volume" style="width: 70%;" string="Volume" class="oe_inline" />
                    <field name="volume_uom_name" style="width: 30%;"
                            class="oe_inline"
                        />
                </div>
                    
                   
                <!-- </div> -->
                <!-- <field name="display_total_weight_in_report" /> -->
                <!-- <field name="display_total_volume_in_report" /> -->
            </field>
        </field>
    </record>

</odoo>
